# Changelog

Wesk의 주요 변경사항을 기록합니다.

## [Unreleased]

### 변경
- 전역 단축키(Alt+W) 소환 기능 제거 — 다시 눌러도 안 들어가는 버그를 blur 가드로 고쳐봤지만 실제로는 단축키 자체가 아예 반응 없는 상태였고(다른 프로그램과 충돌 등 원인 특정이 더 필요했음), 우선 기능을 완전히 빼기로 함. 튜토리얼(첫 실행 안내)에서도 관련 단계 제거(9단계 → 8단계).

### 수정
- 홈페이지 다운로드 버튼이 다른 컴퓨터에서 404 뜨던 버그 수정 — 링크에 옛 버전 파일명(Wesk-1.3.0.exe)이 그대로 박혀있었는데 실제 최신 릴리즈는 1.3.1이라 파일명이 달라서 안 열렸음. 지금 버전에 맞게 고쳤고, 앞으로 버전 올릴 때마다 이 문제가 또 안 생기도록 설치 파일 이름을 버전 안 붙는 고정 이름(Wesk-Setup.exe)으로 바꾸는 빌드 설정도 같이 넣어둠(다음 릴리즈부터 적용, 그때 홈페이지 링크 한 번만 더 갈아주면 됨).
- 스포츠 위젯이 몇 주 전/후 일정까지 넓게 섞어서 보여주던 걸, 끝난 경기 결과는 지금 시각 기준 지난 24시간 이내 것만, 예정 경기는 가까운 순서로 최대 10개까지 보여주도록 범위 조정.
- 첫 실행 안내(튜토리얼) 카드가 너무 작아서 읽기 불편하던 문제 — 카드 크기, 제목/본문 글자 크기 전반적으로 키움.
- 설정 → 레이아웃의 "저장된 레이아웃이 없어요" 같은 빈 상태 문구가 언어를 바꿔도 처음 앱을 켰을 때 언어로 계속 남아있던 버그 수정 — 언어 전환 시 레이아웃 목록도 같이 다시 그리도록 변경.
- "레이아웃 저장"/"이 조합 저장"(커스텀 테마) 버튼을 눌러도 이름 입력창이 안 뜨고 아무 반응이 없던 버그 수정 — 진짜 원인은 Electron 렌더러가 `window.prompt()`(이름 입력받는 네이티브 창)를 아예 지원 안 해서 호출하자마자 예외가 났던 것(alert/confirm은 되는데 prompt만 안 됨). try/catch가 그 예외를 조용히 삼켜서 클릭해도 아무 일도 안 일어나는 것처럼 보였음. 이름 입력이 필요한 곳 전부 앱 안에서 직접 만든 입력창(모달)으로 교체.

## [1.3.1] - 2026-08-29

### 추가
- 설치 마법사에서 언어(한국어/영어) 선택 가능하도록 변경 — 설치 시작하자마자 언어 선택 창이 뜸. (OS 언어를 자동 감지해서 기본값으로 잡아줌)
- 홈페이지(docs/index.html)에 한국어/영어 전환 버튼 추가 — 상단 내비게이션 오른쪽의 언어 버튼을 누르면 페이지 전체(안내 문구, 인터랙티브 데모, 위젯 목록, 다운로드 안내)가 즉시 번역됨. 선택한 언어는 브라우저에 저장돼서 다음 방문 때도 유지됨.
- 앱 내 번역 커버리지 전체 점검 및 보완 — 테마 이름(블루/다크/네온 등), 카운터 위젯 기본 라벨, 뉴스 위젯 출처 표시, 계산기 오류 문구, 차트 위젯 빈 상태 문구, 색상 선택기 복사 문구, 런처 위젯 이름 없음 표시, 클립보드 위젯 이미지 대체 텍스트, 화면 캡처 카운트다운/녹화 버튼 문구, 단위 변환기 단위 이름, 명언 위젯(영어 전용 명언 20개 추가), 스포츠 위젯(리그/팀 이름, 경기 상태, 검색어, 날짜 표시)까지 영어 모드에서 한국어가 남아있던 부분을 전부 찾아서 고침. (환율/주식/날씨 위젯은 원래부터 한국 시장 전용이라 의도적으로 영어 모드 "위젯 추가" 목록에서만 제외됨 — 이미 추가된 위젯은 그대로 동작)

### 수정
- 자동 업데이트 확인이 앱을 켤 때 딱 한 번만 실행되던 문제 수정 — Wesk는 트레이 아이콘 없이 며칠씩 계속 켜놓고 쓰는 경우가 많아서, 실행 중에도 6시간마다 한 번씩 새 버전이 있는지 다시 확인하도록 변경.
- 설정 패널이 내용 많아지면(특히 한국어) 아래쪽이 화면 밖으로 잘려서 안 보이던 문제 수정 — 테마 패널처럼 내용 넘치면 스크롤되도록 변경.

## [1.3.0] - 2026-08-29

### 추가
- 전역 단축키(**Alt+W**)로 위젯 화면을 다른 창들보다 잠깐 앞으로 띄우는 "소환" 기능 추가 — 바탕화면까지 갈 필요 없이 바로 확인 가능. 다른 곳을 클릭하면 자동으로 다시 원래대로 내려감.
- **넛지 알림** 추가 — 안 끝낸 할일이 있으면 몇 시간마다, 오늘이 D-day인 항목이 있으면 하루에 한 번 알려줌. 설정 → "넛지 알림"에서 끌 수 있고, 방해금지 시간대엔 자동으로 조용히 넘어감.
- 첫 실행 안내(튜토리얼)에 단축키·위젯 고정 안내 단계 추가 (총 9단계).
- 위젯 우클릭 메뉴에 **"항상 위에 표시"**(핀) 추가 — 그 위젯 하나만 따로 아주 작은 창으로 떼어내서, 다른 프로그램(브라우저, 게임 등) 위에 항상 떠있게 고정할 수 있음. 고정된 위젯을 다시 우클릭하면 뜨는 "고정 해제"를 누르면 원래 위치·크기 그대로 메인 화면에 돌아옴. 앱을 재시작해도 핀 상태는 그대로 유지됨. (여러 개 동시 선택한 상태에서의 "그룹 핀"과, 핀된 상태에서의 색상/배경/삭제는 아직 지원하지 않음 — 먼저 핀을 해제한 뒤 메인 화면에서 하면 됨)

### 변경
- 트레이 아이콘을 완전히 제거. 위젯 추가·설정·배치 잠금·종료는 이미 화면 상단 바에서 다 할 수 있고(트레이 메뉴와 완전히 겹침), 위젯 화면 소환은 단축키(**Alt+W**)로 통일.

## [1.2.0] - 2026-08-23

### 변경
- 앱 이름을 WCW에서 **Wesk**로 변경 (Widget + Desk). "WCW"가 오래된 미국 프로레슬링 단체 이름과 겹쳐서 검색/SEO에서 전혀 노출이 안 되는 문제가 있었고, 아직 사용자가 거의 없는 지금이 이름을 바꾸기 가장 좋은 시점이라 판단해서 진행. 앱 표시 이름, 실행 파일명, 아이콘 주변 문구, 홈페이지, README, 라이선스 문구 전부 Wesk로 교체.
- GitHub 저장소 이름을 `WCW-widget`에서 `Wesk`로 변경 (그에 따라 홈페이지 주소도 `dortomyork09-cyber.github.io/Wesk/`로 변경됨).
- 기존 사용자 데이터 보호: 앱 내부적으로 쓰던 데이터 저장 폴더 식별자(`wcw`)와 위젯 설정 저장 키는 그대로 유지해서, 업데이트해도 기존에 저장해둔 위젯 배치·설정이 그대로 남아있음.
- 백업 파일 형식도 하위호환 처리 — 리브랜딩 이전에 내보낸 백업 파일(`WCW` 식별자)도 그대로 불러와짐.

## [1.1.1] - 2026-08-22

### 변경
- 공식 배포 방식을 포터블 exe에서 설치형(NSIS)으로 전환 — 포터블은 실행할 때마다 임시폴더에 압축을 풀어서 도는 구조라 경로가 매번 바뀌었고, 그래서 "바탕화면 바로가기 자동 생성"이나 "컴퓨터 켜면 자동 실행" 같은 기능이 안정적으로 동작할 수 없었음(자동실행 등록에 쓰는 경로가 실행할 때마다 달라짐). 설치형으로 바꾸면 고정된 경로에 설치되기 때문에 바탕화면·시작메뉴 바로가기 자동 생성, "컴퓨터 시작 시 자동 실행" 설정, 새 버전 자동 업데이트(백그라운드 다운로드 후 재시작 시 적용)까지 전부 정상 작동함. GitHub Actions 릴리즈 워크플로우가 이제 `npm run build:installer`로 빌드하고, `latest.yml`/`.blockmap`까지 같이 업로드해서 자동 업데이트 체크가 되도록 함. 홈페이지/README 다운로드 안내도 설치 마법사 흐름에 맞게 갱신.

## [1.1.0] - 2026-08-22

### 추가
- 첫 실행 안내를 툴팁 하나짜리에서 게임 튜토리얼처럼 단계별로 짚어주는 방식으로 개편 — 위젯 추가/드래그·크기조절/우클릭 꾸미기/테마/설정/트레이 아이콘까지 실제 버튼을 하나씩 하이라이트하며 총 8단계로 안내. 설정 → "첫 실행 안내 다시보기"에서 언제든 처음부터 다시 볼 수 있음.
- 2048/지뢰찾기/틱택토 위젯 추가 — 방향키로 즐기는 클래식 2048(4x4), 첫 클릭은 항상 안전한 지뢰찾기(9x9·지뢰 10개), 같은 PC에서 둘이 번갈아 두는 틱택토.
- 게임 세션 타이머 위젯 추가 — 시작을 누르면 게임한 시간을 재고, 30/60/90분마다 눈 좀 쉬라는 알림을 소리와 함께 보내줌(끄기 가능).
- 바로가기 위젯에 "디스코드 열기" 버튼 추가 — 설치 경로를 몰라도 바로 실행(디스코드가 안 깔려있으면 안내 문구 표시).
- 설정 패널에 "투명도 통일" 버튼 추가 — 위젯마다 우클릭으로 따로 지정해둔 투명도를 한 번에 지우고, 위쪽 전체 투명도 설정값 하나로 맞춤.

### 수정
- error.log에 찍히는 시각을 UTC(toISOString)에서 이 PC의 로컬 시간대 기준으로 변경 — 로그를 볼 때 실제 시각과 헷갈리지 않도록.
- 2048/지뢰찾기/틱택토 위젯이 뚝뚝 끊겨 보이던 문제 수정 — 칸을 놓거나 움직일 때마다 격자 전체를 지우고 새로 그려서, 색이 바뀌는 애니메이션이 재생될 틈도 없이 그냥 순간이동하듯 바뀌고 있었음. 칸(div)을 한 번만 만들어두고 색/글자만 바꾸도록 고쳐서 부드럽게 전환되도록 개선.
- 지뢰찾기 격자가 거의 안 보이던 문제 수정 — 칸마다 배경색 농도 차이로만 구분하고 있어서 테마에 따라 격자선이 거의 안 보였음. 칸마다 테두리를 직접 그려서 항상 또렷하게 보이도록 수정.
- 위젯을 화면 위쪽으로 드래그하면 갑자기 드래그가 안 먹던 문제 수정 — 맨 위 상단바가 로고/버튼 없는 빈 공간까지도 화면 전체 너비로 클릭을 가로채고 있어서, 위젯을 위로 옮기다 그 46px 띠에 커서가 들어가는 순간 위젯이 아니라 투명한 상단바가 이벤트를 먹어버리고 있었음. 상단바 자체는 클릭을 통과시키고 실제 로고/버튼 부분만 클릭되게 고쳐서 위쪽으로도 자연스럽게 드래그되도록 수정.
- 첫 실행 튜토리얼이 5/6단계에서 안내한 대로 테마·설정 버튼을 실제로 누르면, 패널은 열리지만 튜토리얼 카드가 z-index가 더 높아서 그 위에 그대로 남아 패널 안 버튼(레이아웃 저장 등)을 못 누르는 것처럼 보이던 문제 수정 — 테마/설정 패널을 열 때 튜토리얼이 떠 있으면 같이 닫히도록 수정.
- README·홈페이지에 남아있던 "27개 위젯" 표기를 실제 개수인 31개로 수정 (이번에 추가한 게임 타이머/2048/지뢰찾기/틱택토가 빠져있었음). 홈페이지의 위젯 추가 데모, 상단 흐르는 위젯 이름 목록에도 새 위젯 4개 반영.

### 개선
- 앱 시작 속도: 구글 폰트(Inter/JetBrains Mono)를 <style> 안 @import로 걸어두면 그 폰트를 네트워크로 다 받아올 때까지 화면 자체가 안 그려지는 문제가 있었음(인터넷이 느리거나 방화벽/백신이 첫 요청을 붙잡는 PC에서는 창 뜨는 데만 몇 초씩 걸림) — preload 방식으로 바꿔서 폰트를 백그라운드에서 받아오는 동안에도 시스템 폰트로 화면이 즉시 그려지도록 개선.
- 평소 체감 성능(백그라운드 CPU 사용) 개선: 이 창은 클릭 통과 판정을 위해 전체 가상 데스크톱을 덮고 있어서, WCW를 쓰지 않고 그냥 컴퓨터를 쓰는 동안에도 마우스가 움직일 때마다(게이밍 마우스는 초당 500~1000번!) 어디를 가리키고 있는지 판정하는 코드가 계속 돌고 있었음 — 화면이 실제로 그려지는 빈도(초당 60번 안팎)에 맞춰 한 프레임에 한 번만 판정하도록 묶어서, 마우스를 빠르게 움직여도 불필요한 반복 계산이 쌓이지 않도록 개선(클릭 통과 반응 속도 자체는 그대로).
- 홈페이지(docs/index.html)도 앱과 같은 이유로 구글 폰트를 @import 대신 preload 방식으로 바꿔서 첫 화면이 폰트 로딩을 기다리지 않고 바로 그려지도록 개선.

### 제거
- 성능 모니터 위젯 제거 — 대신 게임 세션 타이머 위젯으로 대체.
- "위젯 추가" 화면의 "최근 사용" 섹션 제거.
- 위젯 배경 이미지 저장을 localStorage에서 IndexedDB로 이전 — 용량 제한 걱정 없이 여러 개 등록 가능(예전 데이터는 자동 이전).
- 위젯 개별 투명도 추가 — 우클릭 → "위젯 투명도"로 위젯마다 따로 투명도 조절.
- 위젯 여러 개 선택(그룹) 추가 — Ctrl+클릭으로 여러 위젯을 골라서 한 번에 같이 이동/잠금.
- "위젯 추가" 화면에 "최근 사용" 섹션 추가 — 최근에 추가했던 위젯을 맨 위에서 바로 다시 선택 가능.
- 설정 패널에 "시작 시 자동 불러오기" 추가 — 앱을 켤 때 지정한 레이아웃으로 자동 시작.
- 알림 소리 종류 선택 추가(기본/차임벨/디지털/부드럽게) — 타이머·뽀모도로·알람 완료음에 공통 적용.
- 할일 위젯에 마감일 지정 기능, 할일/메모에 **굵게** 마크다운 지원. 메모 위젯에 미리보기 모드(체크리스트 `- [ ]` 지원) 추가.
- 뉴스 위젯 RSS 소스를 콤마로 구분해 여러 개 등록 가능(최신순으로 섞어서 표시).
- 캘린더 위젯에 D-day/할일 마감일 표시 추가 — 해당 날짜에 점 표시, 클릭하면 목록으로 확인.
- 트레이 아이콘에 마우스를 올리면 다음 알람/타이머까지 남은 시간 표시.
- 성능 모니터 위젯 추가 — CPU/메모리 사용률을 2초마다 보여줌. 게임하면서 컴퓨터가 버거운지 바로 확인 가능(GPU는 아직 미지원).
- 스네이크 위젯 디자인 업그레이드 — 각진 사각형 대신 둥근 몸통, 머리에 눈, 먹이에 글로우 효과, 은은한 격자 배경 추가.
- 화면 캡처/녹화 위젯 추가 — 스크린샷을 PNG로 저장하고 클립보드에 자동 복사, 화면 녹화는 webm으로 저장 (시스템 오디오 포함 시도, 미지원 환경에서는 화면만 녹화). 저장 위치는 사진 폴더의 WCW 하위 폴더.
- 설정 패널에 "진단 정보" 버튼 추가 — 오류 기록(error.log)이 쌓이는 폴더를 탐색기로 바로 열어볼 수 있음.
- 첫 실행 시 상단 "+ 위젯" 버튼을 안내하는 툴팁 추가 (10초 후 자동으로 사라짐).
- 업데이트 완료 알림 추가 — 새 버전 exe로 실행하면(지난 실행 때와 버전이 다르면) 바탕화면 알림으로 "vX.X.X → vY.Y.Y로 업데이트됐어요" 알려줌. (새 버전이 나왔다는 걸 미리 알려주는 기존 알림과는 별개로, 실제로 업데이트를 끝낸 뒤 확인해주는 알림임)
- 알람 위젯 추가 — 시간을 등록해두면 소리 + 바탕화면 알림으로 알려줌. 여러 개 등록 가능, 개별 켜기/끄기.
- 날씨 위젯 추가 — 도시 이름으로 검색해서 현재 기온/체감/습도/바람/오늘 최고·최저를 보여줌 (open-meteo, API 키 불필요).
- 타이머/뽀모도로 완료 시 소리 + 바탕화면 알림 추가 (뽀모도로는 자연 완료 때만 알려주고, "건너뛰기"로 직접 넘길 땐 조용히 넘어감).
- 트레이 아이콘 우클릭 메뉴에 "위젯 추가", "설정 열기", "배치 잠금 켜기/끄기" 빠른 액션 추가.
- 위젯 드래그 중 다른 위젯 가장자리/중앙에 자석처럼 붙는 정렬 스냅 가이드라인 추가.
- 레이아웃 프리셋 추가 — 지금 위젯 배치를 이름 붙여 저장해두고, 언제든 다른 배치로 바꿔 낄 수 있음 (설정 패널).
- 클립보드 위젯이 이미지도 지원하도록 개선 — 이미지를 복사하면 작은 썸네일로 기록에 남고, 클릭하면 다시 복사됨.
- 뉴스 위젯에 RSS 소스 커스터마이징 추가 — 기본(구글 뉴스) 대신 원하는 RSS 주소를 등록할 수 있음.
- 다중 모니터 지원 — 모니터를 여러 대 쓰거나 배치/해상도를 바꿔도 모든 화면에 위젯을 놓을 수 있도록 창 영역을 자동으로 맞춤.
- 자동 업데이트(NSIS 설치형 빌드 전용) 추가 — electron-updater로 새 버전을 백그라운드에서 받아뒀다가 재시작 시 자동 적용. 포터블 exe는 구조상 지원 불가해 기존 "새 버전 나왔어요" 안내로 유지.
- 영어 UI 지원(1차) 추가 — 설정 패널에서 언어를 한국어/English로 전환 가능. 상단바·설정·테마 패널·위젯 추가 화면·첫 실행 안내가 대상이며, 위젯 내부 라벨은 다음 단계로 남겨둠.
- 화면 언어를 English로 바꾸면 뉴스 위젯의 기본(커스텀 소스 미등록 시) 뉴스가 영어권(미국) 뉴스로 자동 전환.
- 설정 패널에 "첫 실행 안내 다시보기" 버튼 추가 — 최초 1회만 뜨던 상단 안내 툴팁을 언제든 다시 띄워볼 수 있음.
- 화면 언어가 English일 때 환율/주식/날씨는 한국 실정 위젯이라 "위젯 추가" 목록에서 숨김 (이미 추가해둔 위젯은 그대로 유지됨).
- "위젯 추가" 화면에 검색창 추가 — 위젯 이름으로 바로 찾을 수 있음.
- 설정 백업/복원 추가 — 지금 위젯 배치와 설정을 파일로 저장해두고, 나중에(또는 다른 PC에서) 그대로 불러올 수 있음.
- 위젯 개별 잠금 추가 — 우클릭 메뉴에서 위젯 하나만 위치/크기를 고정할 수 있음 (전체 배치잠금과 별개). 잠긴 위젯은 오른쪽 위에 작은 자물쇠 표시가 뜸.
- 영어 UI 지원 2차 — 위젯 내부의 버튼·placeholder·안내 문구·완료 메시지 등도 화면 언어가 English면 함께 번역됨 (날씨 상태, 캘린더 요일/월 표기는 영어권 형식으로 자동 전환).
- 방해금지 시간대 추가 — 설정에서 시작~종료 시각을 정해두면 그 시간엔 알람·타이머·뽀모도로 완료 소리와 알림을 조용히 넘어감.
- 커스텀 테마 추가 — 테마 패널에서 배경/글자/테두리/강조색 2개를 자유롭게 고르고 모서리 둥글기·블러 정도까지 슬라이더로 조절해서 나만의 테마를 만들 수 있음. 여러 조합을 이름 붙여 저장해두고 목록에서 클릭 한 번으로 바꿔 낄 수 있음.
- 위젯 배경 이미지 추가 — 위젯을 우클릭 → "배경 이미지 설정"으로 내 사진/이미지를 위젯 배경으로 깔 수 있음. 글자가 잘 보이도록 지금 테마 색으로 자동 톤다운되고, 재시작해도 유지됨.
- 위젯 개별 색상 추가 — 위젯을 우클릭 → "위젯 색상 설정"으로 위젯 하나하나마다 포인트 컬러(강조색·테두리)를 따로 지정할 수 있음. 전체 테마는 그대로 두고 그 위젯 안에서만 색이 바뀌고, 시계 초침·그래프 막대·스네이크처럼 캔버스로 그려지는 부분도 같이 반영됨. 우클릭 → "위젯 색상 초기화"로 원래대로 되돌리기 가능.

### 수정
- 설정(⚙)/테마 버튼의 아이콘이나 글자를 클릭하면 패널이 열리자마자 바로 닫혀버리던 버그 수정 — 버튼 자신이 아니라 안의 아이콘/텍스트가 클릭 타겟이 될 때 판별 로직이 실패했던 게 원인.
- 위젯 배경 이미지가 거의 안 보일 정도로 톤다운이 너무 진하던 문제 수정 — 사진 위에 얹는 테마색 오버레이 농도를 확 낮춰서 사진이 잘 보이면서 글자 대비도 챙기도록 조정.
- "위젯 추가" 화면 등에서 버튼이 눈에는 보이는데 클릭이 안 먹던 문제 1차 수정 시도 — 상단바/모달/각 패널마다 하나하나 mouseenter·mouseleave로 판정하던 방식을 마우스가 움직일 때마다 커서 바로 아래 요소를 직접 확인하는 방식으로 바꿈(위젯 투명도 패널이 판정 목록에서 빠져있던 것도 같이 고침). 다만 실제로는 이걸로도 문제가 재현돼서 아래 항목으로 근본 원인을 다시 잡음.
- "위젯 추가"를 비롯해 화면 전체가 클릭·호버 모두 완전히 먹통이 되던 문제 — 클릭 통과 기능 쪽을 의심해서 손봤었는데(위 항목), 실제로는 원인이 아니었어서 그 변경은 다시 원복함(오히려 창이 항상 클릭 가능해지면서 이 앱 켜놓은 동안 다른 창을 아예 조작 못 하는 훨씬 심각한 문제가 잠깐 생겼었음 — 죄송).
- 위 문제의 진짜 원인 수정 — `skipAutoSave`, `DEFAULT_SIZE` 두 변수가 스크립트 파일 한참 아래쪽(각각 초기화 관련 코드, 위젯 추가 함수 근처)에 선언돼 있었는데, 그보다 위쪽에 있던 설정 패널 초기화 코드가 스크립트 로딩 도중 그 선언보다 먼저 이 변수들을 참조하면서 "초기화되기 전에 접근했다"는 자바스크립트 오류로 전체 스크립트 실행이 중간에 멈춰버렸음. 이러면 그 오류 지점 이후의 모든 초기화 코드(DEFAULT_SIZE 선언 포함)가 다시는 실행되지 않아서, 위젯 추가 버튼을 아무리 눌러도 매번 같은 오류로 조용히 실패하고 있었음(로그에는 남았지만 화면엔 아무 표시도 없었음) — "위젯 추가가 안 된다"는 신고의 진짜 원인이었음. 두 변수를 스크립트 맨 위, 다른 어떤 코드보다도 먼저 실행되는 위치로 옮겨서 근본적으로 해결.
- 위젯 초기화 도중 어디서 오류가 나든 "위젯 추가" 모달은 무조건 닫히고 오류 내용이 error.log에 남도록 방어 코드 추가 — 이번처럼 원인 파악이 오래 걸리는 걸 방지.

### 개선
- 앱 시작 속도: GPU 셰이더 디스크 캐시 비활성화, 창을 다 그린 뒤에 보여주기(ready-to-show)로 초기 깜빡임 제거.
- 위젯 배경 블러 강도 완화(20px → 14px)로 렌더링 부담 감소.
- 위젯 드래그 방식을 transform 기반으로 변경해 여러 위젯이 떠 있을 때도 부드럽게 움직이도록 개선.
- 위치/크기 자동 저장 주기를 3초 → 15초로 완화 (실제 변경 시점엔 이미 즉시 저장되므로 안전).
- 뉴스/환율/주식/스포츠 위젯의 시작 시점 네트워크 요청을 분산시켜 앱 시작 시 요청이 한꺼번에 몰리지 않도록 개선.
- 알람 위젯의 추가 버튼 아이콘이 과도하게 커지던 문제 수정, 버튼 스타일을 타이머/뽀모도로와 통일.

### 제거
- 전역 단축키(Ctrl+Shift+S 스크린샷, Ctrl+Shift+H 클릭 통과 토글) 기능 제거 — 다른 프로그램 단축키와 충돌 우려로 삭제.

## [1.0.0] - 2026-08

### 추가
- 첫 정식 릴리즈
- 24종 위젯 지원 (시계, 달력, 링크, 할일, 메모, 습관, 카운터, 클립보드, 뉴스, 환율, 주식, 스포츠, 타이머, 뽀모도로, 계산기, 단위변환, D-day, 번역기, 색상 피커, 그래프, 바로가기 런처, 명언, 주사위, 스네이크)
- 위젯 자유 배치 및 크기 조절
- Portable exe 배포 (설치 과정 불필요)
